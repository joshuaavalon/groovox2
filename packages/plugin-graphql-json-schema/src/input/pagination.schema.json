{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/schema/graphql/input/pagination.schema.json",
  "type": "object",
  "title": "Pagination",
  "description": "Control the range of the records to return.",
  "properties": {
    "take": {
      "type": ["integer", "null"],
      "description": "Maximum number of records to return. Must be greater than 0.",
      "minimum": 1
    },
    "skip": {
      "type": ["integer", "null"],
      "description": "Number of records to skip. Must be greater or equal than 0.",
      "minimum": 0
    }
  }
}
